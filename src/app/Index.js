import { View, Text } from 'react-native';
import { APP_TOP_PADDING } from './constants';
import { Header } from '../widgets/Header/Header';

export const Index = () => {
	return (
		<View>
			<Header></Header>
		</View>
	);
};
